/**
 * @deprecated 2026-05-21
 *
 * This generic walker has been replaced by six tool-specific flows
 * (StopFlow, UrgeSurfingFlow, BoxBreathingFlow, GroundingFlow,
 * TippFlow, ThoughtRecordFlow) rendered from start/page.tsx based
 * on the slug.
 *
 * This file is no longer imported anywhere. Safe to delete on Max's
 * Mac via:
 *   rm app/tools/[slug]/start/ToolWalker.tsx
 *
 * It is preserved here only because the sandbox does not have
 * permission to remove host-owned files.
 */
export {};
